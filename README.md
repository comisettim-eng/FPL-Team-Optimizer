## Reproducibility

This project is fully reproducible from a clean environment. All datasets, trained models, and evaluation outputs are generated at runtime and are therefore not tracked in the repository.

To reproduce the full pipeline:

1. Clone the repository  
2. Create the environment using `environment.yml`  
3. Run the main entry point:

```bash
python main.py
